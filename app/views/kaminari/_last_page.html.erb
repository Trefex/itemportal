<% if current_page.last? %>
  <li class="disabled">
    <span>
        <span aria-hidden="true"><%= raw(t 'views.pagination.last') %></span>
      </span>
  <% else %>
  <li>
    <%= link_to raw(t 'views.pagination.last'), url, :remote => remote %>
  <% end %>
</li>
