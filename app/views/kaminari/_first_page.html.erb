<% if current_page.first? %>
  <li class="disabled">
    <span>
        <span aria-hidden="true"><%= raw(t 'views.pagination.first') %></span>
      </span>
  <% else %>
  <li>
    <%= link_to raw(t 'views.pagination.first'), url, :remote => remote %>
  <% end %>
</li>
